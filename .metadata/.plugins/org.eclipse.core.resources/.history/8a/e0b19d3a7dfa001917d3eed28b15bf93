package org.faac.ebb.training;

import io.vertx.core.AbstractVerticle;

public class UserVerticle extends AbstractVerticle {
	private static String USER_PATH = "/user";

	@Override
	public void start() throws Exception {
		super.start();

//		Router router = Router.router(vertx);
//	    router.get(UserVerticle.USER_PATH + "/create").handler(this::createUser);

		vertx.createHttpServer().requestHandler(request -> {
			request.response()
			.putHeader("content-type", "text/plain")
			.end("Hello from User Verticle!");
		}).listen(8080);
	}

//	private void createUser(RoutingContext context, AsyncResult<Void> resultHandler) {
//		resultHandler.
//	}
}
